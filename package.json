{
  "name": "agent-handoff-protocol",
  "version": "0.8.6",
  "description": "Continuity for rotated coding agents: an append-only central-store worklog that leaves tracked project files, the working tree, and Git history untouched.",
  "license": "MIT",
  "author": "Nonosword",
  "type": "module",
  "bin": {
    "ahp": "./bin/ahp",
    "ahp-mcp": "./bin/ahp-mcp"
  },
  "files": [
    "bin/",
    "src/",
    "schema/",
    "SPEC.md",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Nonosword/agent-handoff-protocol.git"
  },
  "keywords": [
    "ahp",
    "agent-handoff",
    "worklog",
    "llm-agent",
    "handoff",
    "claude-code",
    "codex",
    "mcp"
  ],
  "scripts": {
    "test": "node test/run.mjs",
    "verify:examples": "for f in examples/*.jsonl; do node tools/verify-worklog.mjs --file \"$f\" || exit 1; done",
    "schema:check": "node tools/schema-check.mjs examples/*.jsonl"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
