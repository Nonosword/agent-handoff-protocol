# 0.5.11 — Claude Desktop MCP registration test result

## Regression coverage

An isolated installer test supplies a valid Claude Desktop configuration and a
PATH without the `claude` CLI. It verifies that the installer independently
adds the AHP stdio server with Desktop-specific runtime identity, preserves
unrelated preferences and MCP entries, and that uninstall restores the original
document exactly.

## Gate result

- `bash -n install.sh`: passed.
- `npm run schema:check`: passed (20 records valid).
- `npm run verify:examples`: passed (three bundled worklogs; two expected
  recovery notes in the interrupted-session fixture).
- `npm test`: passed (50 passed, 0 failed).
- `git diff --check`: passed.
