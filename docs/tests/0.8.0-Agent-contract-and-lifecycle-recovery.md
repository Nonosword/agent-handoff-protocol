# Tests - Agent contract and lifecycle recovery - 0.8.0

Executed on 2026-09-10 with Node 24.2.0 on macOS.

## Coverage

- unknown explicit Project writes fail without creating hidden store state;
- Project discovery works through CLI JSON and MCP outside a checkout;
- all 12 MCP schemas are closed, fully described and reject unknown/range-invalid
  arguments before invoking the CLI;
- continuation references are validated, and failed done-Lane starts restore the
  prior lifecycle state;
- operator disposition records the exact worklog hash and issues, and permits
  only the guarded completion/archive path;
- Dashboard text and JSON strict exits agree for warnings;
- runtime, Codex, Claude Code and generic-agent guidance retain explicit Project,
  Lane and completion context;
- bundled worklogs continue to validate against the unchanged record schema.

## Final gate

| Check | Result |
| --- | --- |
| `npm test` | Pass — 66 tests, 0 failed. |
| `npm run schema:check` | Pass — 20 records valid against `worklog.schema.json`. |
| `npm run verify:examples` | Pass — all three bundled worklogs verify. |
| `node --check` on runtime, CLI, tools and tests | Pass. |
| `bash -n` on installer and Git hook | Pass. |
| `git diff --check` | Pass. |
| `npm pack --dry-run --json` | Pass — package 0.8.0 contains 22 files. |
| MCP initialize/list/call smoke | Pass — server 0.8.0 exposed 12 tools; Project discovery succeeded; unknown arguments returned `-32602`. |
| Real-PTY `dashboard -w` input/redraw smoke | Pass — repeated up/down escape sequences produced no output or redraw; Ctrl-C restored the alternate screen and exited 0. |
| `install.sh --mode mcp --dry-run --no-color` | Pass — all detected hosts and destination paths were resolved without mutation. |
| Isolated real installer self-test | Pass — the initialize + `tools/list` probe reported 12 tools dynamically. |
