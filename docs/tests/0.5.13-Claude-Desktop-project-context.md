# 0.5.13 — Claude Desktop project context test result

## Regression coverage

The MCP suite starts `ahp-mcp` from a non-repository temporary directory,
simulating a desktop host. It verifies that an empty `ahp_status` call returns
the actionable absolute-cwd/project instruction, that the schema documents the
Desktop requirement, that an explicit absolute checkout succeeds for a
project-scoped Lane listing, and that a relative cwd is rejected.

## Gate result

- `node --check src/mcp.mjs`: passed.
- `npm run schema:check`: passed (20 records valid).
- `npm run verify:examples`: passed (three bundled worklogs; two expected
  recovery notes in the interrupted-session fixture).
- `npm test`: passed (52 passed, 0 failed).
- `git diff --check`: passed.
