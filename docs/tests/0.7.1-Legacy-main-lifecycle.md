# Tests - Legacy main lifecycle - 0.7.1

Executed on 2026-09-10 with Node 24.2.0 on macOS.

| Check | Result |
| --- | --- |
| `npm test` | Pass — 62 tests, including a valid pre-Lane project-wide worklog moving through done, explicit reopen, and archived. |
| `npm run schema:check` | Pass — 20 example records validate against the unchanged JSON Schema. |
| `npm run verify:examples` | Pass — all bundled worklogs validate. |
| `node --check` changed runtime and test modules | Pass. |
| `git diff --check` | Pass. |

The focused regression also proves that materializing legacy lifecycle metadata
does not change `ahp path --lane main`, identity metadata remains immutable,
the reserved `main` route still rejects collisions, and default/all visibility
matches named Lanes.
