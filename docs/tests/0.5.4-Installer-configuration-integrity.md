# 0.5.4 — Installer configuration integrity test result

## Regression scenario

A real non-dry-run installer invocation targets a temporary Cursor MCP
configuration containing both an unrelated MCP server and a top-level host
setting. The test verifies that registration adds only `agent-handoff`, then
that uninstall removes only that entry and restores the original document
content. It also verifies that a malformed configuration remains byte-for-byte
unchanged and that no `.ahp.lock` file is left behind on any path.

## Gate result

- `bash -n install.sh`: passed.
- `npm test`: 47 passed, 0 failed.
- `npm run verify:examples`: passed; expected hard-cutoff notes remain
  non-fatal.
