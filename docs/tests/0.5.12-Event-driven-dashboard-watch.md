# 0.5.12 — Event-driven dashboard watch test result

## Regression coverage

The dashboard test suite verifies that the renderer:

- uses a bold Agent Handoff heading and the `updated at` footer;
- contains no countdown text, one-second sleep loop, or interval timer;
- waits on a filesystem event signal and still uses incremental row redraw plus
  raw/no-echo terminal input; and
- starts a real watcher in an isolated child process, writes a local store file,
  and observes the wake-up event before its timeout.

## Gate result

- `node --check src/dashboard.mjs src/cli.mjs`: passed.
- `npm run schema:check`: passed (20 records valid).
- `npm run verify:examples`: passed (three bundled worklogs; two expected
  recovery notes in the interrupted-session fixture).
- `npm test`: passed (51 passed, 0 failed).
- `git diff --check`: passed.
