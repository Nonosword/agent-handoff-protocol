# Tests - Relocated checkout recovery - 0.8.2

Executed 2026-09-18.

## Regression coverage

- An isolated installation starts with broken absolute `ahp` and `ahp-mcp`
  symlinks pointing into a deleted old checkout. Reinstalling repoints both
  links to the current checkout and `ahp --version` succeeds.
- Live and broken symlinks to a non-AHP `bin/ahp` are retained exactly as-is,
  preserving the installer’s no-clobber guarantee.

## Final gate

| Check | Result |
| --- | --- |
| `npm test` | Pass — 71 tests, 0 failed. |
| `npm run schema:check` | Pass — 20 records valid against `worklog.schema.json`. |
| `npm run verify:examples` | Pass — all three bundled worklogs verify. |
| `node --check` on CLI and test runner | Pass. |
| `bash -n` on installer and Git hook | Pass. |
| `git diff --check` | Pass. |
| `npm pack --dry-run --json` | Pass — package 0.8.2 contains 22 files. |
| `install.sh --mode mcp --dry-run --no-color` | Pass — host discovery and planned registrations complete without mutation. |
