# Tests - Cross-platform host detection - 0.8.1

Executed on 2026-09-10 with Node 22.23.2 on Linux (arm64) with Claude Desktop,
the Codex CLI and the `openai.chatgpt` VS Code extension installed.

## Coverage

- Claude Desktop is detected from its app path alone, and its config is written
  to the platform default (`XDG_CONFIG_HOME` on Linux, `Library` on macOS);
- Codex registers through the binary bundled with the IDE extension, and
  through the binary bundled with the ChatGPT app, with no `codex` on PATH;
- a ChatGPT app without a usable binary yields the manual `config.toml` snippet;
- the Cursor JSON merge regression no longer inherits the host's agent CLIs.

## Final gate

| Check | Result |
| --- | --- |
| `npm test` | Pass — 68 tests, 0 failed. |
| `npm run schema:check` | Pass — 20 records valid against `worklog.schema.json`. |
| `npm run verify:examples` | Pass — all three bundled worklogs verify. |
| `node --check` on runtime, tools and tests | Pass. |
| `bash -n` on installer and Git hook | Pass. |
| `git diff --check` | Pass. |
| `npm pack --dry-run --json` | Pass — package 0.8.1 contains 22 files. |
| `install.sh --mode mcp --dry-run --no-color` | Pass — Claude Desktop detected; Codex CLI and IDE extension reported; ChatGPT desktop reported not found. |
| Isolated installs with fake bundled binaries | Pass — extension-only and app-only runs invoked `codex mcp add agent-handoff --env AHP_WORKER_ID=codex …`; uninstall invoked the same binary. |
