# Tests - Project relocation resilience - 0.8.3

Executed 2026-09-18.

- A remote-backed checkout moved to a new path resolves to its existing Project;
  reads leave registry roots unchanged, while writes append the new root and
  last-seen location.
- A remote-less checkout receives a Git-local UUID only on a write, then keeps
  the same external-store Project and worklog after moving.

| Check | Result |
| --- | --- |
| `npm test` | Pass — 74 tests, 0 failed. |
