# Tests - Review remediation - 0.6.0

Executed on 2026-09-10 with Node 24.2.0 on macOS.

| Check | Result |
| --- | --- |
| `npm test` | Pass — 59 tests, including new Project-collision/checkout, JSONL-boundary, MCP environment, installer TOML/ownership, and missing-store watcher regressions. |
| `npm run schema:check` | Pass — bundled examples validate against the JSON Schema. |
| `npm run verify:examples` | Pass — all example worklogs validate; expected cutoff notes remain non-fatal. |
| `node --check` changed runtime modules | Pass. |
